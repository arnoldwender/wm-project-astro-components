---
/**
 * Tab Component - WenderMedia Astro Components
 *
 * Individual tab button for use within Tabs component.
 *
 * Features:
 * - ARIA role="tab" with aria-controls linked to corresponding TabPanel
 * - Optional leading icon for visual tab identification
 * - Disabled state via aria-disabled attribute
 * - Managed tabindex for roving keyboard navigation within tab list
 * - Slot-based label content for flexible tab text
 *
 * @example
 * <Tab id="overview" icon="📊">Overview</Tab>
 *
 * @example
 * <Tab id="pricing" disabled={false}>Pricing</Tab>
 * <Tab id="reviews" icon="⭐">Reviews</Tab>
 * @copyright 2007-2026 Wender Media - Arnold Wender. MIT License.
 * @see https://github.com/arnoldwender/wm-project-astro-components
 */

export interface Props {
  id: string;
  icon?: string;
  disabled?: boolean;
  class?: string;
}

const {
  id,
  icon,
  disabled = false,
  class: className = '',
} = Astro.props;

const panelId = `${id}-panel`;
---

<button
  type="button"
  role="tab"
  id={`${id}-tab`}
  aria-controls={panelId}
  aria-selected="false"
  aria-disabled={disabled}
  class:list={['tab', className]}
  tabindex="-1"
>
  {icon && <span class="tab-icon">{icon}</span>}
  <slot />
</button>
