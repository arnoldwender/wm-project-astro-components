---
version: 1.0
name: Astro Components
description: "Internal tool / SaaS — astro-components. WM internal tool. Stack varies."

colors:
  white: "#ffffff"
  black: "#000000"
  gray-50: "#f9fafb"
  gray-100: "#f3f4f6"
  gray-200: "#e5e7eb"
  gray-500: "#6b7280"
  gray-700: "#374151"
  gray-900: "#111827"
  success: "#22c55e"
  error: "#ef4444"
  warning: "#eab308" 
---

# Astro Components — DESIGN.md

## Identity

- **Slug:** `astro-components`
- **Repo:** `wm-project-astro-components`
- **Tier:** Internal tool / SaaS
- **Domain:** <unknown>
- **Stack:** Astro ^6.3.1

## Context

WM internal tool. Stack varies.

## Typography

- Typography spec: extract from `src/styles/global.css` `@theme` block when known


## Component Inventory

Extracted from `src/components/` at audit time.

No atomic components found (or not using atomic structure).


## Hard rules (WM network-wide)

- **Light mode default** — `<html data-theme="light">`. NEVER `prefers-color-scheme: dark` auto-detect.
- **Address always Halle HQ** — Franckestraße 3a, 06110 Halle (Saale). Geo lat 51.4825 / lon 11.9706.
- **Email** `mail@{domain}` — NEVER `info@` (except wendermedia.com which IS a real Google Workspace mailbox).
- **No external stock photos** (Pexels/Unsplash/Pixabay/Freepik/Getty/iStock) — only client-owned or Nano Banana editorial.
- **Proprietary license** — NEVER MIT/Apache/GPL/CC BY/CC0.
- **No Co-Authored-By** tags in commits.
- **BFSG 2025 / WCAG 2.1 AA** — alt text every image, 4.5:1 contrast, 44×44 px touch targets.

## Do's

- Use design tokens (`var(--color-*)`, `var(--space-*)`) — never hardcode hex/px values directly in components.
- Semantic HTML — `<header>`, `<nav>`, `<main>`, `<footer>`, `<section>` with meaningful IDs.
- Atomic Design hierarchy — atoms → molecules → organisms → layouts → pages. Never import upward.
- ServiceHero / Hero organism for stadt/branche/leistung pages (in wm-brand-seo-* network).
- FaqAccordion keys: `question` / `answer` (FAQPage schema compatible).
- `<Icon name="..." />` from astro-icon — NEVER `set:html` with icon names.
- Test build before commit: `npm run build` must pass.

## Don'ts

- NEVER hardcode `#hex` / `px` / `rem` / `font-size` in components — use tokens.
- NEVER use Tailwind default palettes for status notices (`bg-yellow-*`, `bg-blue-*`, `bg-emerald-*`) — use semantic `bg-warning-*`, `bg-info-*`, `bg-success-*` tokens.
- NEVER stock images from external providers.
- NEVER fabricate testimonials, reviews, project counts, or ratings (UWG §5 risk).
- NEVER `Co-Authored-By` or "Generated by AI" in commits.

## Agent prompt guide

When generating UI for this brand, the agent should:

1. Read this DESIGN.md first for palette + typography + component inventory.
2. Use existing organism patterns (Hero, CTA, FAQ) — don't rebuild from scratch.
3. Reference `src/data/site.ts` or `src/config/brand.ts` for contact/legal data — NEVER hardcode.
4. Verify accessibility: alt text, semantic HTML, ARIA where needed.
5. Build verify before stopping.

## Implementation

- **Audit script:** `.claude/scripts/design-dna-audit.py --repo .`
- **Audit baseline:** synced to `wm-brand-vault/brands/astro-components/AUDIT-BASELINE.json`
- **Expand sections** (typography spec, component recipes, layout grids, agent prompts) when active development reveals brand-specific patterns. This DESIGN.md is auto-generated v1.0 from repo metadata — refine manually with brand-voice nuance.

## Status

- DESIGN.md v1.0 auto-expanded from repo introspection 2026-05-13
- Replaces previous alpha stub
- Ready for AI-agent consumption + Stitch DESIGN.md spec compliant
